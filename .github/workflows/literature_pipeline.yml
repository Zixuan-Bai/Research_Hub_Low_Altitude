name: Weekly Research Intelligence

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  workflow_dispatch:
    inputs:
      topic:
        description: "Topic key from configs/pipeline.json, topic slug, or all"
        required: false
        default: "all"
  schedule:
    - cron: "0 2 * * 1"

permissions:
  contents: write
  pull-requests: write

jobs:
  collect-weekly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Collect weekly research intelligence
        run: python scripts/collect_weekly.py --topic "${{ github.event_name == 'workflow_dispatch' && inputs.topic || 'all' }}"
      - name: Check for changes
        id: changes
        shell: bash
        run: |
          if [[ -n "$(git status --porcelain)" ]]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Commit changes
        if: steps.changes.outputs.changed == 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          branch="automation/weekly-research-intelligence-${{ github.run_id }}"
          git checkout -b "$branch"
          git add data/items.jsonl outputs/weekly/*.md outputs/review_dashboard.md
          git commit -m "Update weekly research intelligence digest"
          git push -u origin "$branch"
          gh pr create \
            --title "Update weekly research intelligence digest" \
            --body "Automated weekly collection. Review outputs/weekly/ and outputs/review_dashboard.md before merging. This PR does not make final research claims." \
            --base main \
            --head "$branch"
